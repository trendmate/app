import 'package:flutter/material.dart';

class BlogsPage extends StatelessWidget {
  static const routeName = '/blogs-page';
  const BlogsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Blogs Page"),
    );
  }
}
